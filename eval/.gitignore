# bulky / regenerable eval artifacts — keep scripts, manifests, results CSVs, REPORT
inputs/
detail/
detail-*/
bin/
*.in
_smoke*.csv
_e0smoke*.csv
__pycache__/
# trace artifacts if any land here
dependency_*/
*_eval_cubins/
# but DO keep the manifests (document what was run; root .gitignore ignores *.json)
!manifests/
!manifests/*.json
